name: Unity Build | © 2022 by denkhotieu

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Unity - Builder
      uses: game-ci/unity-builder@v2.1.2
